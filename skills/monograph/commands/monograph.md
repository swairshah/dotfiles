---
description: Typeset the requested technical document as a print-quality HTML/PDF monograph
---

Use the `monograph` skill. Write the document described in $ARGUMENTS as Markdown following the skill's house style, save it under `docs/`, render it with `render.py --pdf`, and reply with only the absolute paths of the resulting `.html` and `.pdf` plus a two-sentence summary. Do not paste the document into the terminal.
